# 前端开发服务器配置
# 复制为 .env 或 .env.local 后按需修改

# 监听地址
#   localhost  - 仅本机访问 (http://localhost:5173)
#   0.0.0.0    - 局域网访问，其他设备可通过 http://本机IP:5173 访问
VITE_SERVER_HOST=localhost

# 服务端口
VITE_SERVER_PORT=5173

# API 代理目标（后端 API 地址，代理 /api 请求）
VITE_API_PROXY_TARGET=http://localhost:8000
